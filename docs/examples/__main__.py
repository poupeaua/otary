"""
Main file to generate the result for each of the tutorials.
In practice, the resulted images are then passed through the following command:

pngquant result.png

This is useful for the documentation, as it reduces the size of the images without 
losing quality. The library pngquant is required for this step:
https://pngquant.org/
"""

import os

if __name__ == "__main__":
    pass